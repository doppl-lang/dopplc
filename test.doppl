task(12) myTask {
    #Line comment

    an_int : int

    a_state: {
        an_int : an_int
        finish
    }

    shared data a_byte : byte

    init: {
        maybe another_byte : byte
        internal_state: {
            very_internal_state: {
                a_string = "Hello \"WORLD\" - escapes work \n"
                yield a_string
            }
            once future a_float : float
            a_float = hello(asd:asd)
            lol = {
                finish
            }
        }
    }

    another_state: {
        foo = bar  + lol
        foo = ( !bar + x == y - `(!foo) ) -> a_state
        -> a_state(lol: !(x + y), asd: sd)
    }
     
    state another_string : string

}
